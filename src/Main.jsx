import React from "react";
import ReactDOM from "react-dom";

const MainApp = () => {
  return (
    <div>
      <h1>Princess Oluebube</h1>
    </div>
  );
};

ReactDOM.render(<MainApp />, document.getElementById("app"));

export default MainApp;
